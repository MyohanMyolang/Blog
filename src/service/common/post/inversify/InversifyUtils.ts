// import { container } from "@/service/config/inversify/InversifyConainer";
// import { Container } from "inversify";

// export function useContainer<T>(identifier: symbol, fn: (container: T) => any) {
//   const cont = container.get<T>(identifier);
//   return fn(cont);
// }
